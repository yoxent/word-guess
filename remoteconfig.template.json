{
  "parameters": {
    "levelplay_app_key": {
      "defaultValue": {
        "value": "27c77ea8d"
      },
      "description": "Unused by the app (IDs are compiled in). Kept so a template deploy does not delete the live key.",
      "valueType": "STRING"
    },
    "levelplay_interstitial_id": {
      "defaultValue": {
        "value": "s30cnanav91qppyc"
      },
      "description": "Unused by the app (IDs are compiled in). Kept so a template deploy does not delete the live key.",
      "valueType": "STRING"
    },
    "levelplay_rewarded_extra_rows_id": {
      "defaultValue": {
        "value": "hcokdlvoiili0xya"
      },
      "description": "Unused by the app (IDs are compiled in). Kept so a template deploy does not delete the live key.",
      "valueType": "STRING"
    },
    "levelplay_rewarded_letter_hint_id": {
      "defaultValue": {
        "value": "yz51hy84w8m16lbc"
      },
      "description": "Unused by the app (IDs are compiled in). Kept so a template deploy does not delete the live key.",
      "valueType": "STRING"
    },
    "levelplay_interstitial_frequency_override": {
      "defaultValue": {
        "value": ""
      },
      "description": "Optional override for interstitial frequency capping (reserved; unused in MVP)",
      "valueType": "STRING"
    },
    "min_supported_version": {
      "defaultValue": {
        "value": "1.0.1"
      },
      "description": "Minimum app version (semver). Builds below this see a soft update modal.",
      "valueType": "STRING"
    }
  }
}
